<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 🌈 Dynamic Material You theme (Android 12 +) -->
    <style name="Theme.SmartNotes" parent="Theme.Material3.DynamicColors.DayNight.NoActionBar">
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="android:windowSplashScreenBackground">?attr/colorPrimaryContainer</item>
    </style>

    <!-- Optional: Splash Screen theme -->
    <style name="Theme.SmartNotes.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_gradient</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>

